# Loaded automatically by `vite build --mode embed` (npm run build:embed).
# Skips the login gate entirely — see App.jsx's EMBED_MODE — for mounting
# MOXELA UI inside VideoIPath or another host application.
VITE_EMBED_MODE=true
